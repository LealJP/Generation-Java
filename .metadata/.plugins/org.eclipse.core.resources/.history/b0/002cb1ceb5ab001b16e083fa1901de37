
public class TesteCondicional2 {

	public static void main(String[] args) {
		System.out.println("Testando Condicionais");
		int idade = 20;
		int quantidadePessoas = 3;
		boolean acompanhado = quantidadePessoas >= 2; //variável boolean aceita só 2 valores true ou false
							// (quantidadePessoas >= 2) tem valores possíveis verdadeiro ou falso
		
		if (idade >= 18 || quantidadePessoas >= 2) { // || = or (operador lógico ou)
			System.out.println("Seja bem vindo");
		} else {
			System.out.println("Infelizmente você não pode entrar");	
		}
		
		if (idade >= 18 && acompanhado) { // && = and (operador lógico e)
			System.out.println("Seja bem vindo");
		} else {
			System.out.println("Infelizmente você não pode entrar");	
		}
	}
}
